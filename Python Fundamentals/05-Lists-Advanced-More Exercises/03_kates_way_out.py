# rows = int(input())
# maze = [list(input()) for lines in range(rows)]
# an_exit = ' '
# can_not_escape = False
#
# if (an_exit not in maze[0]
#         and an_exit not in maze[-1]
#         and an_exit not in maze[0][0:-1]
#         and an_exit not in maze[-1][0:-1]):
#     can_not_escape = True
#
#
# if can_not_escape:
#     print(f"Kate cannot get out")